// builtin

// external

// internal

export const DEFAULT_RATING: number = 100;

export interface User {
    user_id: string,
    first_name: string,
    last_name: string,
    username: string,
    rating: number,
}